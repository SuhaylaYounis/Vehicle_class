class Vehicle  //creating a class named vehicle
{
  String model,fuelType,color; //defining the data types of the variables
  double price;
  int cc;

  Vehicle({this.model='',this.fuelType='',this.color='',this.price=0,this.cc=0}); //creating a constructor of vehicle and initializing the variables
}